// The "How It Works" section (features-section with 4-step bento grid) is
// the primary features display in the original HTML. This component is an
// alias so page.tsx can import it under the FeaturesSection name if needed.
export { default } from './HowItWorks'
